/**
 * Core DI Module
 * Contains shared services and utilities that are used across features
 */
import { TYPES } from '../types'

/**
 * Configure core/shared bindings
 * @param {Container} container - The Inversify container
 */
export function configureCoreModule(container) {
  // Add any shared services here in the future
  // Examples:
  // - ValidationService
  // - NotificationService
  // - LoggerService
  // - ConfigurationService
  
  // For now, this module is empty but ready for future shared services
  console.log('Core module configured')
}
